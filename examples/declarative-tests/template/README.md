# greet

Implement `greet()` in [`greet.py`](greet.py) so the program reads a
name from stdin and prints `hello, <name>!`.

```sh
$ echo "Alice" | python3 greet.py
hello, Alice!
```

Run the bundled tests locally with `python3 -m pytest -q` before
submitting. Every push to `main` is autograded.
